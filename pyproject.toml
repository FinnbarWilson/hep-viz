[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hep-viz"
version = "0.1.5"
description = "A local visualizer for HEP data"
readme = "README.md"
authors = [{ name = "Finnbar Wilson", email = "finnbareason@gmail.com" }]
license = { file = "LICENSE" }
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "typer",
    "fastapi",
    "uvicorn",
    "pandas",
    "numpy",
    "pyarrow",
    "datasets",
]
requires-python = ">=3.8"

[project.optional-dependencies]
# fastjet has no wheels on some platforms (e.g. macOS/arm64), so jet
# clustering is opt-in rather than a hard requirement.
jets = ["fastjet"]

[project.scripts]
hep-viz = "hep_viz.cli:app"

[tool.setuptools.packages.find]
where = ["."]

[tool.setuptools.package-data]
hep_viz = ["templates/*.html"]
